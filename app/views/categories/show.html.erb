<h1><%= @category.name %></h1>
<table>
  <tr>
    <th>Post</th>
    <th>Posted date</th>
  </tr>
  <% @category.posts.each do |post| %>
    <tr>
      <td><%= link_to post.title, post_path() %></td>
      <td><%= post.created_at.strftime("%B %d, %Y") %></td>
    </tr>
  <% end %>
</table>